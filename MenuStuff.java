import greenfoot.*;  // (World, Actor, GreenfootImage, Greenfoot and MouseInfo)

/**
 * Write a description of class MenuText here.
 * 
 * @author (your name) 
 * @version (a version number or a date)
 */
public class MenuStuff extends Text
{
    /**
     * Act - do whatever the MenuText wants to do. This method is called whenever
     * the 'Act' or 'Run' button gets pressed in the environment.
     */
    private static final Color transparent = new Color(0,0,0,0);
    private GreenfootImage background;
    public static boolean playsound=true;
    public void act() 
    {
        //GreenfootImage image=new GreenfootImage("Click to start", 50, Color.WHITE, transparent);
        //setImage(image);
    }
   
}
